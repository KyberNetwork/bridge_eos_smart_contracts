* waterloo-bridge package:
-init
-relay
-verify block on longest path (input - block hash)
-check receipt (input - tx + block hash, calls eth-proof package) 

* relay app:
-call init
-call relay

* wknc-app:
all under same api call:
- call verify block on longest path (move functionality from relay app) 
- call check receipt (for now get tx + block hash, later add monitoring for block and tx)
- call issue (calls eth-proof package to get reciept + header rlp, or uses a get service from bridge)



