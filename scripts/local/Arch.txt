* waterloo-bridge package:
-init
-relay
-verify block on longest path (input - block hash)
-check receipt (input - tx + block hash, calls eth-proof package) 

* relay app:
-call init
-call relay

* wknc-app:
all under same api call:
- call verify block on longest path (done - move functionality from relay app) 
- call check receipt (done 0 for now get tx + block hash, later add monitoring for block and tx)
- call issue (currently uses reciept + header rlp from cache)



